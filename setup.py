from setuptools import setup, find_packages

setup(
    description = 'Awarly Python bindings',
    version = '1.0.0',
    author = 'Awarly',
    author_email = 'support@awarly.com',
    packages = find_packages(exclude=['ez_setup']),
)

